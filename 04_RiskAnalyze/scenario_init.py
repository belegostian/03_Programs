from random import randint, shuffle, choice, choices, sample, random, uniform
from collections import defaultdict

# 1. Device List
# Key: device_order, Value: {'type': string,  'fac_group': int, 'net_interface': int}
device_list = {}

